import React from 'react';
import styles from './PlayerScroll.css';
import PropTypes from 'prop-types';
import { enemyList } from '../../data/enemy-list';

const EnemyStats = ({ name, }) => {
  const enemySrc = { goblin, hobgoblin };
}
